# TTV Engine

Text-to-Video Engine for generating videos from text prompts.

## Project Structure

- `backend/`: Python backend for AI/ML processing
- `frontend/`: React frontend for user interface
- `tests/`: Unit and integration tests
- `docs/`: Documentation

## Setup

### Backend
1. Install Python dependencies: `pip install -r requirements.txt`

### Frontend
1. Install Node.js dependencies: `npm install`
2. Start development server: `npm start`

## Contributing

Please follow the 4-day integration plan outlined in the docs.